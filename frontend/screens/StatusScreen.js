import { SafeAreaView, Text } from "react-native";

const StatusScreen = () => {
  return (
    <SafeAreaView>
      <Text>Status Screen</Text>
    </SafeAreaView>
  );
};

export default StatusScreen;
