import { SafeAreaView, Text } from "react-native";

const ProfileScreen = () => {
  return (
    <SafeAreaView>
      <Text>Profile Screen</Text>
    </SafeAreaView>
  );
};

export default ProfileScreen;
