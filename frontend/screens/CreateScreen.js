import { SafeAreaView, Text } from "react-native";

const CreateScreen = () => {
  return (
    <SafeAreaView>
      <Text>Create Screen</Text>
    </SafeAreaView>
  );
};

export default CreateScreen;
